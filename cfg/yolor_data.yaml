# train and val datasets (image directory or *.txt file with image paths)
train: {TRAIN_LIST}  # 118k images
val: {TEST_LIST}  # 5k images
test: {TEST_LIST}  # 20k images for submission to https://competitions.codalab.org/competitions/20794

# number of classes
nc: {CLASSES}

# class names
names: {CLASS_LIST}
